# Product

## Register

product

## Users

TauriTavern 和 SillyTavern 的预设作者、重度调参用户、角色卡作者。他们通常已经拥有多个 OpenAI Settings 预设，需要把某些 PromptManager 条目原封不动地搬到另一个预设里，例如把“小说”“轻小说”一类文风条目放进“夏瑾的文风”分组附近。

他们的工作情境是高密度、重复、容易出错的编辑：需要同时看见来源和目标，比较名称、启用状态、位置和正文，不希望每次都手动复制大段 prompt、切预设、改顺序、再担心保存时覆盖错对象。

## Product Purpose

预设缝合管理器提供一个 TauriTavern 原生感的双栏工作台，用于读取、筛选、复制、收藏和重排 OpenAI 预设中的 PromptManager 条目。它的成功标准是：用户可以在一个界面里完成来源预设到目标预设的条目搬运，并且所有变更在点击保存前都只是未保存修改。

这个产品不是新的预设编辑器，也不是预设仓库。它只聚焦“把已有条目安全搬运到另一个预设”这一件事，并保留原条目的完整字段。

## Brand Personality

原生、克制、可靠。界面应该像 TauriTavern 自己长出来的一块工具面板，而不是另起炉灶的网页应用。它可以清晰、紧凑、有力，但不能抢过预设内容本身的注意力。

## Anti-references

- 不要做营销落地页、英雄区、说明型大卡片或装饰性插画。
- 不要引入独立调色板、紫蓝渐变、玻璃拟态、发光装饰或与酒馆主题冲突的品牌色。
- 不要只依赖拖拽；移动端和键盘用户必须有清楚的按钮路径。
- 不要在选择目标预设后立即写盘；保存必须是显式动作。
- 不要把条目正文改写、格式化或“清理”。搬运就是完整搬运。

## Design Principles

1. 跟随酒馆主题。颜色、边框、文本、强调和背景都来自宿主主题变量，新增样式只做语义映射。
2. 显式保存。复制、删除、排序和启用状态变化都先作为目标预设的未保存修改，保存前必须可撤销或放弃。
3. 双端同等可用。PC 可以高密度并排比较，移动端必须用原生全屏面板和明确按钮完成同样任务。
4. 内容优先。条目名称、启用状态、顺序、角色和正文预览是主角，装饰必须退后。
5. 熟悉胜过惊喜。使用常见的选择框、搜索框、分段标签、图标按钮、列表行和底部保存栏。

## Accessibility & Inclusion

目标为 WCAG AA 方向的实用可达性：键盘可聚焦，按钮有 title 或 aria-label，状态不能只靠颜色表达，移动端触控目标不低于 40px。尊重 reduced motion，动效只用于状态反馈。中文、日文、英文混排文本必须按 UTF-8 保真显示，不允许出现乱码证据仍继续判断。
